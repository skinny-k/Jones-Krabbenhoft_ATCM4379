using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Fertilizer : MonoBehaviour
{
    // [SerializeField] ResourceDrop _fertilizerDropPrefab;
    // [SerializeField] Transform _origin;

    // [SerializeField] float _minValue = 0.75f;
    // [SerializeField] float _maxValue = 1.25f;

    // void FixedUpdate()
    // {
    //     if (Time.time % 1 == 0)
    //     {
    //         ResourceDrop fertilizerDrop = Instantiate(_fertilizerDropPrefab, _origin.position, Quaternion.identity);
    //         fertilizerDrop.Value = Random.Range(_minWaterValue, _maxWaterValue);
    //         fertilizerDrop.Resource = ResourceDrop.ResourceType.Water;
    //     }
    // }
}
